# Cursor Session — 2026-06-16

## Done: Scorecard Guide — Transparency & Admin Weights

Full plan implemented (plan: `.cursor/plans/scorecard_guide_transparency_e01eb413.plan.md` — **do not edit**).

### UI (`/scorecard-explanation`)
- 3 tabs: Fundamental / Technical / Options
- Technical tab shows **V2** (6 self-relative components), not legacy V1
- Columns: Metric | Weight | Direction | Source | Refresh | Coverage | Notes (+ Family on Fundamental)
- Live strips: `DataTransparencyBar` (global FactSet/FMP), `TabStatusHeader` (per-tab refresh/coverage/stale)
- Dynamic Notes suffix when coverage &lt;80%, stale refresh, or sources inactive (`use-guide-status.ts`)
- **Admin only** (`useAuth().isAdmin`): editable weights + sticky Save/Reset; members read-only

### Metadata & weights
- `artifacts/stock-compare/src/lib/scorecard-guide-metadata.ts` — guide rows + `weightGroup`/`weightKey` for admin editing
- `artifacts/stock-compare/src/lib/scoring-weights.ts` — defaults from scorers, merge, validate (family=100, others=1.0)

### API + DB
- Table: `app_config` (`key` text PK, `value` jsonb, `updated_at`) in `lib/db/src/schema/index.ts`
- Migration SQL: `lib/db/sql/app_config.sql` — **must run before API works**
- `GET /api/scoring-config` — public, returns `{ weights }` or null
- `PUT /api/scoring-config` — `requireAdmin`; `{ weights: full config }` or `{ weights: null }` to reset
- Files: `artifacts/api-server/src/routes/scoring-config.ts`, `lib/scoring-config-db.ts`; mounted in `routes/index.ts`

### Live scorer wiring
- `ScoringPreferencesProvider` in `App.tsx` (inside `AuthProvider`); hook: `useScoringPreferences()`
- Overrides injected into:
  - `computeRankingsV2(stocks, familyPreset, fundamentalMetrics)`
  - `computeTechnicalRankingsV2(rows, tierMap, technicalWeights)`
  - `computeOptionScore(..., strikeWeights)` / `pickBestStrike(..., strikeWeights)`
  - `computeStockScore(inputs, optionStockWeights)`
- Consumers: `home.tsx`, `technical.tsx`, `options-scanner.tsx`

### Components
- `artifacts/stock-compare/src/components/scorecard-guide/` — `guide-metric-grid.tsx`, `data-transparency-bar.tsx`, `tab-status-header.tsx`
- `GuideMetricTable` is alias for `GuideMetricGrid`

### Status APIs (existing, unchanged)
- `/api/fundamentals/status`, `/api/technicals/status`, `/api/sdm/status`

### Verify
- tsc clean (stock-compare + api-server); 257 tests pass
- ESLint not configured repo-wide

### Deploy / test checklist
1. `psql $DATABASE_URL -f lib/db/sql/app_config.sql`
2. Restart API server (no hot reload)
3. Admin: edit weights on Scorecard Guide → Save → reload → scores change on Home/Technical/Options Scanner

### Out of scope (per plan)
- Per-user weight profiles (global admin config only)
- Per-metric live timestamps
- Runtime AI gap explanations
- Sidebar/route changes

---

## Earlier same day (Cursor workspace setup)

Token/context optimization for Cursor (parallel to `CLAUDE.md`):
- `.cursorignore`, `.cursor/mcp.json` (codegraph), `.cursor/rules/` (`codegraph.mdc`, `token-efficiency.mdc`, `cursor.mdc`)
- `CURSOR.md` entry doc; hooks: `.cursor/hooks.json` + `session-start.js` / `rehydrate-cursor.js`
- Cursor-only session log: `.cursor/context/session.md` (separate from `.agents/`)

## For other agents

- **Single source of truth for guide display:** `scorecard-guide-metadata.ts`; **for runtime weights:** `scoring-weights.ts` + DB `app_config` key `scoring_weights`
- Admin saves send **full merged** `ScoringWeightsConfig`, not partial patches
- Fundamental family weights are 0–100; technical/options are 0–1 fractions (UI shows % for latter)
- If `GET /api/scoring-config` 500s, `app_config` table likely missing — run SQL migration
- Options tab coverage is static labels ("On scan" / "DB daily/weekly"), not live %
