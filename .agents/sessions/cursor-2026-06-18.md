# Cursor Session — 2026-06-18

## Done: Watchlist Stock Analysis Enhancements

Watchlist click → `StockBreakdown` panel extended with cache-first competitors + catalysts.

### UI (`stock-breakdown.tsx`)
- **Company Overview** — drops first bullet when it duplicates header company name
- **Competitors** — `CompetitorsSection` (watchlist mode only): top 5 peers ranked by combined score, `#rank TICKER score` rows
- **Event Risk / Catalysts** — `CatalystsSection`: concise bullet list from breakdown payload

### Scoring
- `computeTechFundScore` in `stock-scorer.ts` — **50/50** technical + fundamental (no options/relative-move/tag)
- Server mirror in `peer-rankings.ts` + `technical-scorer-v2.ts` (V2 self-relative tech score from DB rows)

### API (cache-first)
- `GET /api/stocks/competitors/:ticker` — DB-only scores; peers from `ticker_registry` → industry/sector → one-time FMP stock-peers + bidirectional `peer_tickers[]` merge
- `POST /api/stocks/competitors/backfill` — auth; stale/missing only; batched FMP fundamentals + technicals (3 concurrent); watchlist insert
- Breakdown extended: `calendarEvents` on existing Yahoo call; `buildCatalysts()` → `catalysts[]` in 2h cache
- Caches: `peersCache` (24h), `profileCache` (24h) registered in `cache-registry.ts`

### New files
- `artifacts/api-server/src/lib/{catalysts,peer-resolver,peer-rankings,technical-scorer-v2}.ts`
- `artifacts/stock-compare/src/components/{competitors-section,catalysts-section}.tsx`
- `use-watchlist.ts`: `COMPETITOR_TAG_COLOR` (#64748b), `addEntryIfMissing`

### Verify
- `npm run typecheck` in `artifacts/stock-compare` — pass

### Manual test checklist
1. Watchlist → click ticker → no duplicate name in overview bullets
2. Competitors section shows ranked peers (or "Scoring…" then scores after backfill)
3. Catalysts section shows earnings/analyst/news bullets
4. Missing peer tickers appear on watchlist with slate tag

### Next
- options-comparison-table (still in progress per state.md)
- Smoke-test competitors on Replit with sparse `ticker_registry`

### Session wrap
- Resolved `state.md` rebase conflict (active work: fildi-social-feed / options-comparison-table)
- Updated `api-endpoints.md` with competitors routes + peer-map caches
